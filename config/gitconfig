[core]
    editor = nvim
	pager = delta
    #pager = diff-so-fancy | less --tabs=4 -RFX
[interactive]
    diffFilter = delta --color-only
[delta]
    syntax-theme = Nord
    navigate = true
    line-numbers = true
[pull]
    rebase = false
[merge]
    #tool = vimdiff
    conflictstyle = diff3
[diff]
    #tool = vimdiff
    colorMoved = default
[includeIf "gitdir:{dir}"]
    path = ./private-config
